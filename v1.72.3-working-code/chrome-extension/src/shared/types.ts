/**
 * Marco Extension — Shared Types (Re-export)
 *
 * Canonical source: src/shared/types.ts
 */
export type {
    HealthState,
    MatchMode,
    ExecutionWorld,
    InjectionMethod,
    RunAt,
    InjectionConditions,
    MatchResult,
    ScriptBindingResolved,
    RegexValidation,
} from "../../../src/shared/types";
